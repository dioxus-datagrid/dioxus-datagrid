# Minimal stand-in for a real user project. Deliberately NOT a workspace member:
# `dx components add` must work against a project that knows nothing about this repo.
[package]
name = "consumer-app"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
dioxus = { version = "0.7.10", features = ["web"] }
